<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme base mínimo. El theming real lo hace Compose en RaizTheme. -->
    <style name="Theme.RAIZ" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
